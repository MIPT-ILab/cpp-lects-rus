#include <assert.h>
#include <math.h>
#include <stdio.h>
#include <stdlib.h>

struct frep {
  unsigned exp;
  unsigned frac;
};

struct frep
as_frep(float f) {
  struct frep res;
  // TODO: your code here
  return res;
}

int
main() {
  // TODO: input 2 numbers: x, y
  //       output 4 numbers: explow, fraclow, exphigh, frachigh
}
